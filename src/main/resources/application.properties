#server.port=80

# ===============================
# = DATA SOURCE
# ===============================

# Set here configurations for the database connection

# Connection url for the database "netgloo_blog"
spring.datasource.url = jdbc:mariadb://localhost:3306/emailAttacher
#spring.datasource.url = jdbc:mysql://localhost:3306/emailAttacher?useSSL=false
# Username and password
spring.datasource.username = root
spring.datasource.password = root

# Keep the connection alive if idle for a long time (needed in production)
#spring.datasource.testWhileIdle = true
#spring.datasource.validationQuery = SELECT 1

spring.datasource.driver-class-name= org.mariadb.jdbc.Driver
#spring.jpa.database-platform= org.hibernate.dialect.MySQL8Dialect  
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# ===============================
# = JPA / HIBERNATE
# ===============================

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto = none

# Naming strategy
#spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# Allows Hibernate to generate SQL optimized for a particular DBMS
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

#spring.jpa.open-in-view=true

#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDB103Dialect

#cat localhost.crt localhost.key > localhost.pem
#openssl pkcs12 -export -out localhost.p12 -inkey localhost.pem -in localhost.pem
#Abans Andorra 8443

#server.port=8444 
#server.ssl.enabled=true

#server.ssl.key-alias=mailattacher
#sudo keytool -list -v -keystore mailattacher.p12 -storetype PKCS12 |grep Alias Este comando te dice el alias si te situas en el terminal en la carpeta de resources del workpaces

#server.ssl.key-alias=1
#server.ssl.key-store=src/main/resources/mailattacher.p12
#server.ssl.key-store=classpath:mailattacher.p12

#server.ssl.key-store-type=pkcs12
#server.ssl.key-store-password=caca




#server.port=8443
#server.ssl.enabled=true
#server.ssl.key-alias=localhost
#server.ssl.key-store=src/main/resources/localhost.p12
#server.ssl.key-store-type=pkcs12
#server.ssl.key-store-password=caca

#server.ssl.key-password=caca

spring.servlet.multipart.max-file-size=10000MB
spring.servlet.multipart.max-request-size=10000MB
#server.tomcat.max-swallow-size=-1
#server.tomcat.relaxed-query-chars=|,{,},[,],^
logging.level.org.springframework.web=DEBUG



 